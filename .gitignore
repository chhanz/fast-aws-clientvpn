# Internal design document
DESIGN-PLAN.md

# Per-environment config (may contain sensitive values)
clientvpn.conf

# Certificate / key artifacts (CERT_DIR="$SCRIPT_DIR/clientvpn-certs" in mgt-clientvpn.sh)
# Certificates are generated next to the script, inside this repo, so these
# patterns keep them out of version control.
clientvpn-certs/
*.pem
*.key
*.crt
*.ovpn

# easy-rsa clone directory (CERT_DIR/easy-rsa)
easy-rsa/
