# Internal design document
DESIGN-PLAN.md

# Per-environment config (may contain sensitive values)
clientvpn.conf

# Certificate / key artifacts (CERT_DIR="$HOME/clientvpn-certs" in mgt-clientvpn.sh)
# CERT_DIR lives under $HOME, outside this repo, but ignore these patterns
# in case artifacts are ever copied or generated inside the working tree.
clientvpn-certs/
*.pem
*.key
*.crt
*.ovpn

# easy-rsa clone directory (CERT_DIR/easy-rsa)
easy-rsa/
